# Put these values in apps/web/.env.local. Hosted values come from the
# Supabase project settings; local values are printed by `pnpm supabase:start`.
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=replace-with-local-anon-or-publishable-key

# Legacy name also supported when migrating an existing local environment:
# NEXT_PUBLIC_SUPABASE_ANON_KEY=replace-with-local-anon-key

# Server/worker only. Never expose this value to the extension or browser.
SUPABASE_SECRET_KEY=replace-with-server-secret-key

# Legacy server key name is also supported:
# SUPABASE_SERVICE_ROLE_KEY=replace-with-legacy-service-role-key

# Server only. Use the managed receiving domain shown in Resend, such as
# cool-hedgehog.resend.app, or a verified custom inbound subdomain.
INBOUND_EMAIL_DOMAIN=replace-with-your-resend-receiving-domain.resend.app

# Server only. Use at least 32 random characters for inbound adapter authentication.
INBOUND_EMAIL_WEBHOOK_SECRET=replace-with-a-long-random-secret

# Server only. Required by POST /api/v1/webhooks/resend. Create the API key and
# email.received webhook in Resend. Point the webhook at:
# https://your-public-app.example/api/v1/webhooks/resend
RESEND_API_KEY=re_replace-with-resend-api-key
RESEND_WEBHOOK_SECRET=whsec_replace-with-resend-webhook-signing-secret

# Optional local-only mode. The worker polls Resend so no public webhook or web
# hosting is required. Minimum 10000; defaults to 30000 milliseconds.
RESEND_POLL_INTERVAL_MS=30000

# Local development only. Never enable the simulator in production.
ENABLE_EMAIL_SIMULATOR=false
EMAIL_SIMULATOR_BASE_URL=http://127.0.0.1:3000
